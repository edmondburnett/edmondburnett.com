# edmondburnett.com

A blog for essays on code/tech/linux/ops.

### Usage

Serve:

`cargo run`
`cargo run -- serve --port 8080`

Validate posts:

`cargo run -- validate`

Run unit tests:

`cargo test`
