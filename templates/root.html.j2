{% extends "layout.html.j2" %}
{% block content %}

Hello {{ name }}!

{% for post in posts %}
    <h2>{{ post.title }}</h2>
    <p>{{ post.description }}</p>
    <p>{{ post.formatted_date() }}</p>
{% endfor %}

{% endblock %}
